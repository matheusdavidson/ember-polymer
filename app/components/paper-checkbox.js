import Ember from 'ember';

export default Ember.Component.extend({
	tagName: 'paper-checkbox',
	// didInsertElement: function(){
	// 	var html = this.$().find('.comp-content').detach();

	// 	this.$().find('#checkboxLabel').prop('hidden', false).html(html);
	// 	console.log('elem', this.$().find('#checkboxLabel')[0]);
	// }
});
